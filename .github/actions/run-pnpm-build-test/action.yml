name: 'Build and Test'
description: 'Builds and tests the application with linting'
inputs:
  run-build:
    description: 'Run build step'
    required: false
    default: 'true'
  run-test:
    description: 'Run test step'
    required: false
    default: 'true'
  run-lint:
    description: 'Run lint step'
    required: false
    default: 'true'
  run-typecheck:
    description: 'Run type checking'
    required: false
    default: 'true'
  run-format:
    description: 'Run format check'
    required: false
    default: 'false'
  build-command:
    description: 'Custom build command'
    required: false
    default: 'build'
  test-command:
    description: 'Custom test command'
    required: false
    default: 'test --passWithNoTests'
  lint-command:
    description: 'Custom lint command'
    required: false
    default: 'lint'
  typecheck-command:
    description: 'Custom typecheck command'
    required: false
    default: 'typecheck'
  format-command:
    description: 'Custom format check command'
    required: false
    default: 'format:check'
  working-directory:
    description: >
      Directory to run the checks in (the Node project root). Required when the
      project is not at the repo root — a caller job's
      `defaults.run.working-directory` does NOT reach inside a composite action.
    required: false
    default: '.'

runs:
  using: 'composite'
  steps:
    - name: Run Format Check
      if: inputs.run-format == 'true'
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      run: pnpm run --if-present ${{ inputs.format-command }}

    - name: Run Type Checking
      if: inputs.run-typecheck == 'true'
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      run: |
        TYPECHECK_SCRIPT="${{ inputs.typecheck-command }}"

        # Skip only when the script is missing from package.json.
        if ! node -e "const scripts = require('./package.json').scripts || {}; process.exit(scripts[process.argv[1]] ? 0 : 1);" "${TYPECHECK_SCRIPT}"; then
          echo "Type checking script '${TYPECHECK_SCRIPT}' not found, skipping"
          exit 0
        fi

        echo "Running type checking via '${TYPECHECK_SCRIPT}'..."
        pnpm run --silent "${TYPECHECK_SCRIPT}"
        echo "Type checking passed"

    - name: Run Linting
      if: inputs.run-lint == 'true'
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      run: pnpm run --if-present ${{ inputs.lint-command }}

    - name: Run Build
      if: inputs.run-build == 'true'
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      run: |
        echo "Building application..."
        pnpm run ${{ inputs.build-command }}
        echo "Build completed successfully"
    
    - name: Run Tests
      if: inputs.run-test == 'true'
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      run: |
        pnpm run --if-present ${{ inputs.test-command }}
